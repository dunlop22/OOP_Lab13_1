#include "SendedStateCont.h"

void SendedStateCont::putParcel(Parcel* parcel)
{
	cout << "\n\nПосылку невозможно поместить в контейнер, т.к. он уже отправлен.\n\n";
	//сделать вывод: не кладём такую-то посылку в такой-то контейнер, тк контейнер уже в пути
}
